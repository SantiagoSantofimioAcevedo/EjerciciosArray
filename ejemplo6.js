// Crear dos arrays con diferentes elementos
let grupo1 = [1, 2, 3, 4];
let grupo2 = ['a', 'b', 'c', 'd'];

// Combinar los dos arrays en un solo array nuevo manteniendo el orden original
let Combinado = [...grupo1, ...grupo2];

console.log("Array combinado:");
console.log(Combinado);